var arr = [2, 3, 4, 5, 6, 8]; // array whose type is annotated to number
for (var _i = 0, arr_1 = arr; _i < arr_1.length; _i++) {
    var ele = arr_1[_i];
    console.log(ele);
}
var brr = [2, 5, 7, 8, "medhaj"]; // array whose type is annotated to string and number
for (var _a = 0, brr_1 = brr; _a < brr_1.length; _a++) {
    var ele = brr_1[_a];
    console.log(ele);
}
